extends layout

block content
 div(class="" x-data)
  input(id="user" type="hidden" value=user)
  input(id="memories" type="hidden" value=memories)
  button(id="addm" @click = "alert('dd')" class="rouded fixed bottom-10 right-10 border-2 shadow") New memory
  div(class="p-2 m-2 shadow ")
   each memory in memories
    memorylist-item(memory=memory)
 script(src="/javascripts/pages/memories.js")